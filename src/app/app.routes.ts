import { Routes } from '@angular/router';
import { SellerRegistration } from './pages/seller-registration/seller-registration';

export const routes: Routes = [{ 
    path: '', component: SellerRegistration }];
